:imagesdir: ../../images
:favicon: {imagesdir}/favicon.ico
:copyright: PhaseShift Studio
:author: Marko A. Rodriguez
:email: marko@markorodriguez.com
:revnumber: 0.1-alpha
:icons: font
:icon-set: fas
:stem: latexmath
:source-highlighter: highlight.js
:highlightjsdir: ../highlight
:highlightjs-languages: mtron,html,bash,json
:stylesheet: ../css/metatron.css
// :linkcss:
:data-uri:
:toc-title: metatron
:toc: left
:toclevels: 4
:license-url: https://www.gnu.org/licenses/agpl-3.0.html
:license-title: AGPLv3
:lang: en
:max-width: 85%
:app-name: metatron
:version-label: ring-oriented computing
:!revdate:
:!revremark:
:!revnumber:
:docname: metatron documentation
:doctitle: metatron
:!author:
:!email:
:L: latexmath

    mtron> ring-oriented computing

++++
<script src="https://unpkg.com/highlightjs-copy/dist/highlightjs-copy.min.js"></script>
<link rel="stylesheet" href="https://unpkg.com/highlightjs-copy/dist/highlightjs-copy.min.css"/>
++++

image:metatron-character-small.png[width=200,float=left]
image:github-icon.png[width=20,link=https://github.com/phaseshift-studio/metatron] source

[.metatron] can be understood as

 1. a multi-model [.red]*database query language*
 2. a data-oriented [.red]*operating system*.

image:m-arch.svg[width=400,float=right] These representations are made salient by a ring-oriented programming language and virtual machine. As an example, within the metatron, an mtron-trained large language model is prompted to generate a multi-model expression to search multiple database (e.g. key/value, vector, graph), to then insert those results into a multiple web documents stored on multiple machines in a jQuery-esque manner, whereby a dynamically spawned web server then provides a single point of access to fetching the distributed documents to yield a single view via a browser.

[quote]
____
The ways in which metatron can be used to generate, shape, relate and distribute data is endless. This flexibility is made possible by the integrated computing environment known as 'the metatron.'
____

To understand [.metatron] it is necessary to understand the core mtron language and the mechanics of the underlying virtual machine.

### metatron architecture

The fundamental architecture of [.metatron] is realized as a network of coupled https://en.wikipedia.org/wiki/Ring_(mathematics)[algebraic rings] executing all levels of abstraction -- from the human-readable/writeable programming language, to the compiler that generates a sequence of instructions for the virtual machine to evaluate using a swarm of atomic monadic processors that ultimately read and write data identified by a ring-based id system supported by a heterogeneous set of data stores called __spaces__. Each of these components follows the axioms of an algebraic ring (or some subset algebra such as a monoid or group).

#### algebraic ring theory

A ring is a set of elements latexmath:[R] along with two binary operators 

[stem]
++++
+\colon R \times R \rightarrow R
++++
and 
[stem]
++++
*\colon R \times R \rightarrow R,
++++
where latexmath:[*] distributes over latexmath:[+] such that for latexmath:[a,b,c \in R],
[latexmath]
++++
\begin{align*}
a*(b+c) =\;& (a * b) + (a * c) \\
        =\;& (ab)    + (ac) \\
        =\;& ab      + ac
\end{align*}
++++
A useful corollary allows for an analogous transformation of polynomial rings, whereby
[latexmath]
++++
\begin{align*}
(a+b)*(c+d) =\;& ((a+b)*c) + ((a+b)*d) \\
            =\;& (ac+bc) + (ad+bd) \\
            =\;& ac+bc+ad+bd.
\end{align*}
++++
Moreover, latexmath:[R] contains two unique elements latexmath:[0 \in R] and latexmath:[1 \in R] such that, for any latexmath:[a \in R],
[latexmath]
++++
\begin{align*}
0 + a &= a & a + 0 &= a \\
0 + a &= 0 & a + 0 &= 0 \\
1 + a &= a & a + 1 &= a.
\end{align*}
++++
Finally, every element latexmath:[a \in R] has is an _inverse_ latexmath:[-a \in R] such that
[stem]
++++
\begin{align*}
0 =\;& a + (-a) \\
  =\;& a - a.
\end{align*}
++++
A ring is denoted
[stem]
++++
<R,+,-,0,1>.
++++

These are the axioms of ring theory and any collection of tokens, processes, objects, identifiers, etc. that obey these rules form, by definition, a ring. 

#### the rings of metatron

There are many rings in metatron. Of particular importance is the ring fURI -- a "fhat" Uniform Resource Identifier. `fURIs` are used to denote the type, extension, and location of an `obj` (object) in metatron. In fact, an `obj` in metatron is composed of three elements, one data structure and 2 `fURIs`, where:
++++
\begin{align*}
\texttt{obj} \in (\texttt{jvm} \times \texttt{tid} \times \texttt{vid}),
\end{align*}
++++
where `jvm` is an object of the Java virtual machine, `tid` is a _type_ (class) `fURI` and `vid` is a _value_ (instance) `fURI`. Every `obj` in metatron has a form, a denotation of that form, and an reference to it's explicit location in metatron.

#### fURI




```
a+0 = a
a*1 = a
a-a = 0
a*(b+c) = ab + ac 
(b+c)*a = ba + ca
```

include::intro.adoc[]

== instruction sets
include::instset/mtron.adoc[]
include::instset/mkv.adoc[]
include::instset/mweb.adoc[]
include::instset/mgrph.adoc[]
include::instset/mvec.adoc[]
include::instset/mllm.adoc[]
include::instset/mach.adoc[]
