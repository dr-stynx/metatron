=== mach instruction set

image:neno-newt-phuud-drawbridge.png[width=500,float=left,role="parchment"]

image:the-crew-and-wagon.png[width=500,float=left,role="parchment"]

image:the-crew-campfire.png[width=500,float=left,role="parchment"]

image:sir-beepboopbop-bzzz.png[width=500,float=left,role="parchment"]

The `mach` instruction set provides `objs` enabling the reflective processing of the executing virtual machine. In particular, `mach` (machine) and `monad` are accessible via the `insts` made available.

```mtron
1.drop()
```

